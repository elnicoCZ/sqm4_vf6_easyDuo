// EasyDuo demo media configuration file.
media =
{
  video = ( { name     = "SQM4K70 Production";
              pipeline = "filesrc location=/home/media/sqm4k70_480x272.avi ! tee name=a  a. ! queue ! avidemux ! ffdec_mpeg4 ! videorate drop-only=true ! autoconvert ! fbdevsink device=/dev/fb2  a. ! queue ! avidemux ! ffdec_mp3 ! volume volume=1.0 ! alsasink"; },
            { name     = "PCBuilder Presentation";
              pipeline = "filesrc location=/home/media/pcbuilder_480x272.avi ! tee name=a  a. ! queue ! avidemux ! ffdec_mpeg4 ! videorate drop-only=true ! autoconvert ! fbdevsink device=/dev/fb2  a. ! queue ! avidemux ! ffdec_mp3 ! volume volume=1.0 ! autoaudiosink"; },
            { name     = "Big Buck Bunny";
              pipeline = "filesrc location=/home/media/big_buck_bunny_480x272.avi ! tee name=a  a. ! queue ! avidemux ! ffdec_mpeg4 ! videorate drop-only=true ! autoconvert ! fbdevsink device=/dev/fb2  a. ! queue ! avidemux ! ffdec_mp3 ! volume volume=1.0 ! osssink"; }
          );

  camera = ( { name     = "USB Camera";
               pipeline = "v4l2src device=/dev/video0 ! video/x-raw-yuv,width=640,height=480, framerate=30/1 ! videorate drop-only=true ! autoconvert ! fbdevsink device=/dev/fb2"; }
           );
};
